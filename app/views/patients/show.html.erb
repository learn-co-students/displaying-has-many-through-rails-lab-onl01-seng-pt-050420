<h1><%=@patient.name%> Appointments: </h1>


<ul>
    <% @patient.appointments.each do |a| %>
    <li>
        <%= link_to a.doctor.name, doctor_path(a.doctor)%><br>
        <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M")%><br><br>
        </li>
    <% end %>
</ul>