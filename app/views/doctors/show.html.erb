<h1><%= @doctor.name %></h1>

<ul><% @doctor.appointments.each do |apt| %>
    <li><%= link_to apt.patient.name, patient_path(apt.patient)%> - <%= apt.appointment_datetime.strftime('%B %d, %Y at %k:%M')%>
    <% end %>
</ul>