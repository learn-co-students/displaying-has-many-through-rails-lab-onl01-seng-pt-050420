<h2><%= @doctor.name %></h2>
<% @doctor.appointments.each do |apt| %>
    <%= link_to "#{apt.appointment_datetime.strftime('%B %d, %Y at %H:%M')} - #{apt.patient.name}", patient_path(apt.patient) %>
<% end %>